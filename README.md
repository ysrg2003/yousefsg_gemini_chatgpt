
# 🚀 AAPI Pro Hub | الترسانة الموحدة للذكاء الاصطناعي

هذا المشروع ليس مجرد واجهة محادثة، بل هو **نظام متكامل للهندسة العكسية وتحويل المتصفحات إلى واجهات برمجية (API)**. يتيح لك استخدام نماذج الذكاء الاصطناعي (**Gemini** و **ChatGPT**) مجاناً عبر استغلال قوة حساباتك الشخصية وسيرفرات **GitHub Actions**، دون الحاجة لدفع سنت واحد أو القلق من حظر الـ API التقليدي.

سواء كنت مطوراً تريد دمج الذكاء الاصطناعي في تطبيقك، أو مستخدماً عادياً يريد تجربة ذكاء اصطناعي "خارقة" ومجانية، هذا هو الدليل الشامل لك.

---

## ✨ مميزات النظام السحابي
* **تجاوز الحمايات (Anti-Bot):** يستخدم محرك `Camoufox` (المبني على Playwright) بميزات تخفي بشرية كاملة لتخطي حواجز Cloudflare و Google.
* **بنية تحتية سحابية:** يعتمد على GitHub Actions كـ "خوادم بدون رأس" (Headless Servers)، مما يعني استقراراً عالياً ومجانية كاملة.
* **سرعة الاستجابة (Zstd Compression):** يتم ضغط الترسانة البرمجية والمتصفحات بتقنية Zstd لضمان فك الضغط وتشغيل النظام في أقل من 15 ثانية داخل السيرفر.
* **التزامن الذكي (Parallelism):** بفضل نظام `Request ID` الفريد، يمكنك إرسال 10 طلبات في وقت واحد وسيقوم النظام بتوجيه كل رد إلى صاحبه بدقة.
* **واجهة أمامية (Dynamic UI):** ملف `index.html` متطور يتواصل مباشرة مع GitHub API لمراقبة حالة الطلبات وعرض الردود بتنسيق Markdown الجذاب.

---

## 🛠️ المتطلبات الأساسية
قبل البدء، تأكد من توفر:
1.  **حساب GitHub.**
2.  إضافة **[Cookie-Editor](https://cookie-editor.com/)** مثبتة على متصفحك.
3.  تسجيل الدخول في [ChatGPT](https://chatgpt.com) و [Google Gemini](https://gemini.google.com).

---

## 📖 دليل التشغيل الشامل (خطوة بخطوة)

### 1️⃣ الخطوة الأولى: نسخ المشروع (Fork)
اضغط على زر **Fork** في أعلى الصفحة لإنشاء نسخة خاصة بك. هذه النسخة هي "سيرفرك الخاص" الذي سنتعامل معه.

### 2️⃣ الخطوة الثانية: تفعيل الصلاحيات (Crucial)
GitHub يغلق صلاحيات الكتابة افتراضياً. لتفعيلها:
1.  اذهب إلى **Settings** -> **Actions** -> **General**.
2.  انزل إلى **Workflow permissions** واختر **Read and write permissions**.
3.  اضغط **Save**.

### 3️⃣ الخطوة الثالثة: سحب الكوكيز (السر المهني)(اختياري)
المتصفح السحابي يحتاج "هويتك" ليدخل المواقع.
* افتح Gemini/ChatGPT.
* افتح Cookie-Editor، اضغط **Export** ثم **Export as JSON**.
* **هام:** لا تشارك هذه الملفات مع أحد، فهي تحتوي على صلاحية الدخول لحسابك.

### 4️⃣ الخطوة الرابعة: إعداد الأسرار (GitHub Secrets)(اختياري)
1.  في مستودعك: **Settings** -> **Secrets and variables** -> **Actions**.
2.  اضغط **New repository secret** وأضف:
    * `GEMINI_COOKIES`: (الصق JSON الخاص بـ Gemini).
    * `GPT_COOKIES`: (الصق JSON الخاص بـ ChatGPT).

### 5️⃣ الخطوة الخامسة: بناء الترسانة (مرة واحدة فقط)
هذا النظام يحتاج متصفحات ومكتبات ضخمة. قمنا باختصارها في ملف واحد:
1.  اذهب إلى تبويب **Actions**.
2.  اختر **🚀 Build & Release Permanent Assets**.
3.  اضغط **Run workflow**. انتظر حتى يكتمل البناء (سيظهر إصدار v1.0.0 في الـ Releases).

---

## 🔑 استخدام النظام كذكاء اصطناعي (API) للمطورين

هنا تكمن القوة الحقيقية. النظام مصمم ليعمل كـ **Backend API** لتطبيقاتك.

### الفلسفة التقنية: النظام "غير متزامن" (Asynchronous)
بما أن GitHub Actions تستغرق وقتاً للتشغيل، لا يمكنك الحصول على رد في أجزاء من الثانية. دورة الطلب هي:
1.  **الإرسال (Trigger):** ترسل الـ Prompt مع `request_id` فريد.
2.  **المعالجة:** يفتح السيرفر المتصفح، يسأل الذكاء الاصطناعي، ويحفظ الرد في ملف (Artifact).
3.  **الاستلام (Polling):** يقوم تطبيقك بفحص المستودع، وعند اكتمال الطلب، يحمل ملف النتيجة.

### 📡 روابط الاستدعاء (Endpoints)
يجب إرسال طلب `POST` إلى الروابط التالية باستخدام GitHub Token الخاص بك:

* **Gemini API:**
    `https://api.github.com/repos/{USER}/{REPO}/actions/workflows/gemini_api.yml/dispatches`
* **ChatGPT API:**
    `https://api.github.com/repos/{USER}/{REPO}/actions/workflows/gpt_api.yml/dispatches`

### 📝 مثال استدعاء باستخدام cURL:
```bash
curl -X POST \
  -H "Accept: application/vnd.github.v3+json" \
  -H "Authorization: token YOUR_GITHUB_TOKEN" \
  [https://api.github.com/repos/USER/REPO/actions/workflows/gemini_api.yml/dispatches](https://api.github.com/repos/USER/REPO/actions/workflows/gemini_api.yml/dispatches) \
  -d '{
    "ref": "main",
    "inputs": {
      "prompt": "اكتب لي كود بايثون لتشفير النصوص",
      "request_id": "unique_id_99"
    }
  }'

🐍 مكتبة Python كاملة لاستخدام النظام (SDK)
هذا الكود يمثل "العميل" الذي تضعه في تطبيقك ليتحدث مع النظام:
import requests, time, zipfile, io, json, uuid

class AAPI_Pro_Client:
    def __init__(self, token, owner, repo):
        self.headers = {"Authorization": f"Bearer {token}", "Accept": "application/vnd.github.v3+json"}
        self.base_url = f"[https://api.github.com/repos/](https://api.github.com/repos/){owner}/{repo}"

    def ask_ai(self, prompt, engine="gemini"):
        workflow = "gemini_api.yml" if engine == "gemini" else "gpt_api.yml"
        req_id = str(uuid.uuid4().hex)[:10] # معرف فريد للطلب
        
        # 1. إرسال السؤال للسيرفر
        trigger_url = f"{self.base_url}/actions/workflows/{workflow}/dispatches"
        data = {"ref": "main", "inputs": {"prompt": prompt, "request_id": req_id}}
        
        print(f"📡 إرسال الطلب لـ {engine}...")
        res = requests.post(trigger_url, headers=self.headers, json=data)
        if res.status_code != 204: return f"Error: {res.text}"

        # 2. الانتظار وجلب النتيجة (بحد أقصى 2.5 دقيقة)
        artifact_name = f"{engine}-json-{req_id}"
        print("⏳ جاري المعالجة سحابياً... (30-60 ثانية)")
        
        for attempt in range(30):
            time.sleep(5)
            # فحص أحدث العمليات المكتملة
            runs_url = f"{self.base_url}/actions/runs?status=completed&per_page=5"
            run_data = requests.get(runs_url, headers=self.headers).json()
            
            for run in run_data.get("workflow_runs", []):
                # جلب ملفات النتائج (Artifacts) الخاصة بالعملية
                arts_res = requests.get(run["artifacts_url"], headers=self.headers).json()
                for art in arts_res.get("artifacts", []):
                    if art["name"] == artifact_name:
                        print("📥 تم استلام الرد! جاري التحميل...")
                        zip_res = requests.get(art["archive_download_url"], headers=self.headers)
                        with zipfile.ZipFile(io.BytesIO(zip_res.content)) as z:
                            file_name = f"{engine}_result.json"
                            with z.open(file_name) as f:
                                return json.loads(f.read().decode('utf-8'))['response']
        
        return "Timeout: السيرفر استغرق وقتاً طويلاً."

# --- مثال تشغيل ---
# api = AAPI_Pro_Client("ghp_TOKEN", "MY_USER", "MY_REPO")
# print(api.ask_ai("اشرح لي نظرية النسبية باختصار", engine="chatgpt"))

⚠️ حل المشكلات (Troubleshooting)
 * ردود فارغة أو خطأ في الكوكيز: الكوكيز تنتهي صلاحيتها. إذا توقف النظام، قم بتصدير كوكيز جديدة من المتصفح وتحديثها في الـ Secrets.
 * فشل التحميل (Artifact Not Found): تأكد من أن الـ request_id الذي ترسله هو نفسه الذي تبحث عنه. الكود أعلاه يتعامل مع هذا تلقائياً.
 * توقف العمليات (Actions): تأكد من أن حساب GitHub الخاص بك لم يتجاوز الـ 2000 دقيقة مجانية (وهو رقم ضخم جداً للاستخدام الشخصي).
> ملاحظة: تم تطوير هذا النظام لتسهيل الوصول للنماذج الذكاء الاصطناعي برمجياً. يرجى احترام سياسات الاستخدام الخاصة بـ Google و OpenAI.
> 

